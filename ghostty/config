# ==============================================================================
# Ghostty Terminal Configuration
# Location: ~/.config/ghostty/config
# ==============================================================================

# ------------------------------------------------------------------------------
# 剪贴板与选择设置 (Clipboard & Selection)
# ------------------------------------------------------------------------------
# 开启划选即自动复制。macOS 上 clipboard 才会写入系统剪贴板，
# 不要写成 true，否则只会写入 Ghostty 的私有剪贴板。
copy-on-select = clipboard

# ------------------------------------------------------------------------------
# 字体设置 (Font Configuration)
# ------------------------------------------------------------------------------
font-family = Menlo
font-size = 14
